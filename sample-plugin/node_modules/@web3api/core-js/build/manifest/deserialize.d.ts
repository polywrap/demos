import { Manifest } from "./";
export interface DeserializeOptions {
    noValidate?: boolean;
}
export declare function deserializeManifest(manifest: string, options?: DeserializeOptions): Manifest;
