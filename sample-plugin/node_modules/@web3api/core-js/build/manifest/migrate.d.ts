import { AnyManifest, Manifest, ManifestFormats } from "./formats";
declare type Migrator = {
    [key in ManifestFormats]?: (m: AnyManifest) => Manifest;
};
export declare const migrators: Migrator;
export declare const migrateManifest: (manifest: AnyManifest, to: ManifestFormats) => Manifest;
export {};
